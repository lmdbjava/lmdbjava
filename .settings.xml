<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <servers>
    <server>
      <id>bintray-dist-maven</id>
      <!-- populated from .travis.yml env: settings -->
      <username>${env.BINTRAY_USER}</username>
      <password>${env.BINTRAY_PASS}</password>
    </server>
  </servers>
</settings>
